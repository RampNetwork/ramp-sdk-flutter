# Ramp Network Flutter example

Demo app for [`ramp_flutter`](../): configure widget URL params, present the
Ramp WebView in a bottom sheet, and log `WidgetEvent` / host replies.

```sh
flutter run
```
