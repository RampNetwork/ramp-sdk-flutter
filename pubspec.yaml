name: ramp_flutter
description: "Ramp Network for Flutter loads the Ramp widget in a Flutter WebView with a unified Dart API for iOS and Android."
version: 5.0.0
homepage: https://ramp.network/

environment:
  sdk: ^3.12.2
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^10.1.2
  image_picker: ^1.1.2
  url_launcher: ^6.3.2
  # Fork with NavigationDelegate.onCreateWindow (target=_blank / window.open).
  # Android / wkwebview are listed because we import them; same commit as webview_flutter.
  # https://github.com/mateusz-ramp/flutter_packages/tree/webview-target-blank
  webview_flutter:
    git:
      url: https://github.com/mateusz-ramp/flutter_packages.git
      ref: a37d157a9160fbd509484e9bb176be038c4d153c
      path: packages/webview_flutter/webview_flutter
  webview_flutter_android:
    git:
      url: https://github.com/mateusz-ramp/flutter_packages.git
      ref: a37d157a9160fbd509484e9bb176be038c4d153c
      path: packages/webview_flutter/webview_flutter_android
  webview_flutter_wkwebview:
    git:
      url: https://github.com/mateusz-ramp/flutter_packages.git
      ref: a37d157a9160fbd509484e9bb176be038c4d153c
      path: packages/webview_flutter/webview_flutter_wkwebview

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
